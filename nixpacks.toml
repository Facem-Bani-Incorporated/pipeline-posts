[phases.setup]
nixPkgs = ["python311", "imagemagick"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m streamlit run main.py --server.port $PORT --server.address 0.0.0.0"